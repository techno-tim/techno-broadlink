import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#051a1f',
    },
    secondary: {
      main: '#0398b6',
    },
    error: {
      main: '#E73023',
    },
    success: {
      main: '#4BAF4B',
    },
  },
});
export default theme;
